# Dependencies
node_modules/
**/node_modules/

# Build outputs
dist/
build/
**/dist/
**/build/
*.log

# Environment variables & secrets (CRITICAL - never commit)
.env
.env.local
.env.*.local
*.env
**/.env
**/.env.local
**/.env.*.local

# Firebase & service account keys (CRITICAL - never commit)
serviceAccount*.json
*-firebase-adminsdk-*.json
**/serviceAccount*.json
**/*-firebase-adminsdk-*.json
firebase-sa.json
**/firebase-sa.json
.secrets/
**/.secrets/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# OS
.DS_Store
Thumbs.db

# Prisma
*.db
*.db-journal
prisma/migrations/
!prisma/migrations/migration_lock.toml

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
tmp/
temp/
*.tmp

# Uploaded files
uploads/
**/uploads/
# Proof photos — never web-served, and must never be committed either.
uploads-private/
**/uploads-private/

# Coverage
coverage/
*.lcov
.nyc_output/

# Testing
.nyc_output/
coverage/

# TypeScript
*.tsbuildinfo

# Backend specific
backend/dist/
backend/.env
backend/*.db
backend/*.db-journal

# Local database (SQLite, WAL mode).
# These MUST stay ignored: scripts/deploy.sh runs `git stash push -u`, which
# sweeps up untracked files but leaves ignored ones alone. An unignored
# children.db would be stashed away mid-deploy — i.e. the live database would
# vanish on the next push to main.
backend/data/
**/*.db-wal
**/*.db-shm

# Frontend specific
web/dist/
web/.env
frontend/dist/
frontend/.env
backend/logs/
frontend/build/
deploy.tar.gz
mobile/
